mutation Mutation($input: LoginUserType) {
  login(input: $input) {
    token
    user {
      fullname
      email
      two_factor_enabled
      account_status
      role
    }
    errors {
      message
      code
    }
  }
}