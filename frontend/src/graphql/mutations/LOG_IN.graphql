mutation Mutation($input: LoginUserType) {
  login(input: $input) {
    token
    user {
      fullname
      email
      two_factor_enabled
      account_status
      role
      is_verified
    }
    errors {
      message
      code
    }
  }
}